@using System.Configuration;
@using System.Globalization;
@using System.Xml;
@using Microsoft.AspNetCore.Html;
@inject AdvanceCRM.MultiTenancy.ITenantAccessor TenantAccessor
@using System.IO;

<!DOCTYPE html>
@*<style>    h1 {
            text-align: right;
        }

    button {
      margin: 5px;
    }

    #punch-out, #bye {
      display: none;
    }

    .btn-default {
      color: #bcbcbc;
    }

    .fa-undo, .fa-redo {
      margin-right: 10px;
    }</style>*@

@{
    Func<string, IHtmlContent> json = x => new HtmlString(Serenity.JSON.Stringify(x));
    var hideNav = Context.Request.Query["hideNav"] == "1";
    var logged = Serenity.Authorization.Username;
    var themeCookie = Context.Request.Cookies["ThemePreference"];
    var theme = !string.IsNullOrEmpty(themeCookie) ? themeCookie : "azure-light";
    var darkSidebar = theme != null && theme.IndexOf("light") < 0;
    var rtl = System.Globalization.CultureInfo.CurrentUICulture.TextInfo.IsRightToLeft;
    var user = (UserDefinition)Serenity.Authorization.UserDefinition;
    var tenantName = TenantAccessor?.CurrentTenant?.CompanyName;
    var userImage = (user == null || string.IsNullOrEmpty(user.UserImage)) ? "data:image/png;base64,"
        : Url.Content("~/upload/" + user.UserImage);
    var headerTitle = !string.IsNullOrWhiteSpace(tenantName)
        ? tenantName.Trim()
        : Texts.Navigation.SiteTitle.ToString();

    var sessionTimeout = 20; //minutes
    var userIdValue = user?.UserId ?? 0;
    var UICulture = System.Globalization.CultureInfo.CurrentUICulture.Name;
}
<html lang="@UICulture" class="no-js@(hideNav ? " no-navigation" : "")" @Html.Raw(rtl ? " dir=\"rtl\"" : "")>
<head>
    <script>var AdvanceCRM = window.AdvanceCRM || {};</script>
    <script src="~/Scripts/jquery-3.5.1.min.js" asp-append-version="true"></script>
    <script src="~/Scripts/bootstrap.bundle.min.js" asp-append-version="true"></script>
    <script src="~/Scripts/adminlte/app.js" asp-append-version="true"></script>
    @{
        Html.RenderPartial(MVC.Views.Shared._LayoutHead);
    }
    @RenderSection("Head", false)
    <title>@(ViewData["Title"]) - @Texts.Site.Layout.WhiteLabel CRM</title>
</head>
<body id="s-@(ViewData["PageId"])Page" class="@(ViewData["BodyClass"]) fixed sidebar-mini hold-transition skin-@theme @(darkSidebar ? "dark-sidebar" : "light-sidebar")@(hideNav ? " no-navigation" : " has-layout-event")@(rtl ? " rtl" : "")">
    <div id="PageBackground" style="display: none"></div>
    @if (hideNav)
    {
        <script type="text/javascript">
            $(function () {
            @RenderSection("PageInitScript", false)
            });
        </script>
        <div id="page-outer-nonav">
            <div id="page-container" class="page-container-common">
                @RenderBody()
            </div>
        </div>
    }
    else
    {
        <div class="wrapper">
            <header class="main-header">
                <a href="~/" class="logo">
                    <span class="logo-mini"><i></i></span>
                    <span class="logo-lg">
                        <i></i>
                        <b id="TenantHeaderTitle">@headerTitle</b>
                    </span>
                    @if (user != null && user.CompanyId > 0)
                    {
                        <script type="text/javascript">
                            $(function () {
                                AdvanceCRM.Administration.CompanyDetailsService.Retrieve({
                                    EntityId: @user.CompanyId
                                }, function (response) {
                                    try {
                                        var entity = response && response.Entity;
                                        if (entity && entity.Name) {
                                            $('#TenantHeaderTitle').text(entity.Name);
                                        }
                                    } catch (e) {
                                        console.warn('Failed to apply tenant company name', e);
                                    }
                                }, {
                                    blockUI: false,
                                    onError: function (error) {
                                        console.warn('Failed to retrieve tenant company name', error);
                                        return false;
                                    }
                                });
                            });
                        </script>
                    }
                    <script type="text/javascript">
                        window.SessionTimeout = (function() {
                            var _timeLeft, _popupTimer, _countDownTimer;

                            var stopTimers = function() {
                                window.clearTimeout(_popupTimer);
                                window.clearTimeout(_countDownTimer);
                            };

                            var updateCountDown = function() {
                                var min = Math.floor(_timeLeft / 60);
                                var sec = _timeLeft % 60;
                                if(sec < 10)
                                    sec = "0" + sec;

                                document.getElementById("CountDownHolder").innerHTML = min + ":" + sec;

                                if(_timeLeft > 0) {
                                    _timeLeft--;
                                    _countDownTimer = window.setTimeout(updateCountDown, 1000);
                                }
                                else
                                {
                                    window.location.replace = "/Account/Signout";
                                }
                            };

                            var showPopup = function() {
                                _timeLeft = 60;
                                updateCountDown();
                                ClientTimeoutPopup.Show();
                            };

                            var schedulePopup = function() {
                                stopTimers();
                                _popupTimer = window.setTimeout(showPopup, @((sessionTimeout - 1) * 60000));
                            };

                            var sendKeepAlive = function() {
                                stopTimers();
                                ClientTimeoutPopup.Hide();
                                SessionTimeout.schedulePopup();
                            };

                            return {
                                schedulePopup: schedulePopup,
                                sendKeepAlive: sendKeepAlive
                            };

                                                })();


                    </script>

                </a>
                <nav class="navbar navbar-static-top" role="navigation">
                    <a href="#" class="sidebar-toggle" data-toggle="offcanvas" role="button">
                        <span class="sr-only">Toggle navigation</span>
                    </a>

                    @{
                        string configFile = System.IO.Path.Combine(AppContext.BaseDirectory, "ExternalAppSetting.config");
                        XmlDocument xml = new XmlDocument();
                        xml.Load(configFile);

                        foreach (XmlNode node in xml.SelectNodes("/appSettings/add"))
                        {
                            string key = node.Attributes["key"].Value;
                            string value = node.Attributes["value"].Value;
                            ConfigurationManager.AppSettings.Set(key, value);
                        }

                        var EndDate = ConfigurationManager.AppSettings["EndDate"].ToString();
                    }

                    <p class="nav navbar-brand" style="font-size:14px; font-weight:500;">
                        @if (EndDate.IsNullOrEmpty() == false)
                        {
                            if (((Convert.ToDateTime(EndDate, CultureInfo.InvariantCulture)).Subtract(DateTime.Now).TotalDays <= 7) && ((Convert.ToDateTime(EndDate, CultureInfo.InvariantCulture)).Subtract(DateTime.Now).TotalDays >= 0))
                            {
                                int remday = (int)DateTime.Now.Subtract(Convert.ToDateTime(EndDate, CultureInfo.InvariantCulture)).TotalDays * -1;
                                <text><marquee>Your license is going to expire in @remday days</marquee> </text>
                            }
                        }
                    </p>
                    <div class="navbar-custom-menu">
                        <ul class="nav navbar-nav">

                            <!-- Add Punch In and Punch Out buttons here -->
                            @*<li>
            <a href="javascript:;" class="btn btn-success navbar-btn" onclick="punchIn()" style="padding: 5px 10px; font-size: 12px;">
                <i class="fa fa-sign-in"></i> Punch In
            </a>
        </li>
        <li>
            <a href="javascript:;" class="btn btn-danger navbar-btn" onclick="punchOut()" style="padding: 5px 10px; font-size: 12px;">
                <i class="fa fa-sign-out"></i> Punch Out
            </a>
        </li>

        <li class="dropdown notifications-menu">
            @{Html.RenderPartial(MVC.Views.Shared.Notifications, new AdvanceCRM.Notifications.NotificationsModel()); }

        </li>*@

                            <li class="d-flex justify-content-center align-items-center flex-column my-3" style="padding-top: 8px; padding-right: 8px;">
                                <button class="btn btn-success mb-2 px-4 py-2" id="punchInButton" data-toggle="tooltip" data-placement="bottom" title="Punch In" onclick="punchIn()">
                                    <i class="fa fa-clock-o"></i> Punch In
                                </button>
                                <button class="btn btn-danger px-4 py-2" id="punchOutButton" data-toggle="tooltip" data-placement="bottom" title="Punch Out" onclick="punchOut()" style="display: none;">
                                    <i class="fa fa-clock-o"></i> Punch Out
                                </button>
                            </li>

                            <li>
                                <a href="~/Mailbox/Inbox?flag=Inbox&Search=&page=1&min=0&max=0&prepageNo=1" data-toggle="tooltip" data-placement="bottom" title="Mailbox">
                                    <i class="fa fa-envelope"></i>
                                </a>
                            </li>
                            <li>
                                <a href="~/Calendar" data-toggle="tooltip" data-placement="bottom" title="Calendar">
                                    <i class="fa fa-calendar"></i>
                                </a>
                            </li>

                            <li class="dropdown user user-menu">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                    <img src="@userImage" class="user-image" alt="User Image">
                                    <span class="hidden-xs">@Serenity.Authorization.Username</span>
                                </a>
                                <ul class="dropdown-menu">
                                    <!-- User image -->
                                    <li class="user-header">
                                        <img src="@userImage" class="img-circle" alt="User Image">

                                        <p>
                                            @(user != null ? user.DisplayName : "")
                                        </p>
                                    </li>

                                    <!-- Menu Footer-->
                                    <li class="user-footer">
                                        <div class="pull-left">
                                            <a href="~/Account/ChangePassword" class="btn btn-default btn-flat"><i class="fa fa-lock fa-fw"></i> @Texts.Forms.Membership.ChangePassword.FormTitle</a>
                                        </div>
                                        <div class="pull-right">
                                            <a href="~/Account/Signout" class="btn btn-default btn-flat"><i class="fa fa-sign-out fa-fw"></i> @Texts.Navigation.LogoutLink</a>
                                        </div>
                                    </li>
                                </ul>
                            </li>

                            <li>
                                <a href="#" data-toggle="control-sidebar"><i class="fa fa-sliders"></i></a>
                            </li>
                        </ul>
                    </div>
                </nav>
            </header>

            <aside class="main-sidebar">
                <section class="sidebar">
                    <form action="#" method="get" class="sidebar-form">
                        <div class="input-group">
                            <input type="text" id="SidebarSearch" name="q" class="form-control" autocomplete="off">
                            <span class="input-group-btn">
                                <button type="button" name="search" id="search-btn" class="btn btn-flat"><i class="fa fa-search"></i></button>
                            </span>
                        </div>
                    </form>
                    <ul class="sidebar-menu" id="SidebarMenu">
                        @{
                            Html.RenderPartial(MVC.Views.Shared.LeftNavigation, new AdvanceCRM.Navigation.NavigationModel());
                        }
                    </ul>
                </section>
            </aside>

            <div class="content-wrapper">
                @if (IsSectionDefined("ContentHeader"))
                {
                    <section class="content-header">
                        @RenderSection("ContentHeader")
                    </section>
                }
                <section class="content">
                    @RenderBody()
                </section>
            </div>

            <footer class="main-footer">
                <div class="pull-right hidden-xs">
                    @Html.Raw(Texts.Site.Layout.FooterInfo)
                </div>
                <strong>@Texts.Site.Layout.FooterCopyright</strong> @Texts.Site.Layout.FooterRights
            </footer>

            <aside class="control-sidebar control-sidebar-dark">
                <div class="tab-content">
                    <div class="tab-pane active" id="control-sidebar-settings-tab">
                        <form method="post">
                            <h3 class="control-sidebar-heading">@Texts.Site.Layout.GeneralSettings</h3>

                            <div class="form-group">
                                <label class="control-sidebar-subheading">@Texts.Site.Layout.Language</label>
                                <select id="LanguageSelect" class="form-control"></select>
                            </div>

                            <div class="form-group" style="margin-top: 15px;">
                                <label class="control-sidebar-subheading">@Texts.Site.Layout.Theme</label>
                                <select id="ThemeSelect" class="form-control"></select>
                            </div>
                        </form>
                    </div>
                </div>
            </aside>
            <div class="control-sidebar-bg"></div>
        </div>


        <!-- jQuery Script to Handle Click Events -->
        @*<script>
                function punchIn() {
                    Q.confirm("Are you sure you want to punch in?", () => {
                        Q.serviceCall({
                            service: 'Attendance/PunchIn',
                            request: {},
                            onSuccess: () => {
                                Q.notifySuccess("Punched in successfully!");
                            },
                            onError: () => {
                                Q.notifyError("Failed to punch in.");
                            }
                        });
                    });
                }

                function punchOut() {
                    Q.confirm("Are you sure you want to punch out?", () => {
                        Q.serviceCall({
                            service: 'Attendance/PunchOut',
                            request: {},
                            onSuccess: () => {
                                Q.notifySuccess("Punched out successfully!");
                            },
                            onError: () => {
                                Q.notifyError("Failed to punch out.");
                            }
                        });
                    });
                }

            </script>*@






        @*<script>
                function getAddressFromCoordinates(lat, lon, callback) {
                    // Use OpenStreetMap Nominatim API for free reverse geocoding
                    let url = `https://nominatim.openstreetmap.org/reverse?format=json&lat=${lat}&lon=${lon}`;

                    fetch(url)
                        .then(response => response.json())
                        .then(data => {
                            if (data.display_name) {
                                callback(data.display_name);
                            } else {
                                callback("Unknown Location");
                            }
                        })
                        .catch(error => {
                            console.error("Geocoding error:", error);
                            callback("Unknown Location");
                        });
                }

                function punchIn() {
                    if (navigator.geolocation) {
                        navigator.geolocation.getCurrentPosition((position) => {
                            let latitude = position.coords.latitude;
                            let longitude = position.coords.longitude;
                            let coordinates = latitude + "," + longitude;

                            getAddressFromCoordinates(latitude, longitude, (location) => {
                                Q.serviceRequest('Attendance/PunchIn', {
                                    Entity: {
                                        Coordinates: coordinates,
                                        Location: location // Send the actual location
                                    }
                                }, (response) => {
                                    Q.notifySuccess("Punched in successfully!");
                                }, {
                                    onError: (err) => {
                                        Q.notifyError(err.Message);
                                    }
                                });
                            });
                        }, (error) => {
                            Q.notifyError("Unable to fetch location: " + error.message);
                        });
                    } else {
                        Q.notifyError("Geolocation is not supported by this browser.");
                    }
                }

                function punchOut() {
                    if (navigator.geolocation) {
                        navigator.geolocation.getCurrentPosition((position) => {
                            let latitude = position.coords.latitude;
                            let longitude = position.coords.longitude;
                            let coordinates = latitude + "," + longitude;

                            Q.serviceRequest('Attendance/PunchOut', {
                                Entity: {
                                    Coordinates: coordinates,
                                    Location: "Fetching..."
                                }
                            }, (response) => {
                                Q.notifySuccess("Punched out successfully!");
                            }, {
                                onError: (err) => {
                                    Q.notifyError(err.Message);
                                }
                            });
                        }, (error) => {
                            Q.notifyError("Unable to fetch location: " + error.message);
                        });
                    } else {
                        Q.notifyError("Geolocation is not supported by this browser.");
                    }
                }
            </script>*@
        <script>
                     document.addEventListener("DOMContentLoaded", function () {
                         CheckPunchStatus(); // Check status on page load

                document.getElementById("punchInButton").addEventListener("click", function () {
                    if (navigator.geolocation) {
                        navigator.geolocation.getCurrentPosition(function (position) {
                            var latitude = position.coords.latitude;
                            var longitude = position.coords.longitude;
                            var coordinates = latitude + "," + longitude;

                            var apiKey = "YOUR_GOOGLE_API_KEY"; // Replace with your actual API key
                            var location = "Office";
                            var userId = @userIdValue; // Ensure Razor renders this correctly

                            var apiUrl = `/api/Leads/PunchIn?UserId=${userId}&Location=${encodeURIComponent(location)}&Coordinates=${encodeURIComponent(coordinates)}`;

                            $.ajax({
                                url: apiUrl,
                                type: 'POST',
                                success: function (response) {
                                    alert(response);
                                    CheckPunchStatus(); // Refresh button states
                                },
                                error: function (xhr, status, error) {
                                    alert("Error: " + error);
                                }
                            });

                        }, function (error) {
                            alert("Geolocation error: " + error.message);
                        });
                    } else {
                        alert("Geolocation is not supported by this browser.");
                    }
                });

                document.getElementById("punchOutButton").addEventListener("click", function () {
                    var userId = @userIdValue;
                    var apiUrl = `/api/Leads/PunchOut?UserId=${userId}`;

                    $.ajax({
                        url: apiUrl,
                        type: 'POST',
                        success: function (response) {
                            document.getElementById("punchInButton").style.display = "none";
                        },
                        error: function (xhr, status, error) {
                            alert("Error: " + error);
                        }
                    });
                });
            });

                        function CheckPunchStatus() {
                var userId = @userIdValue;

                fetch(`/api/Leads/CheckPunchStatus?UserId=${userId}`)
                    .then(response => response.text())
                    .then(data => {
                        console.log(data);
                        if (data === "PunchedIn") {

                            document.getElementById("punchInButton").style.display = "none";
                           document.getElementById("punchOutButton").style.display = "inline-block";
                        }
                    });
            }

        </script>
        <script type="text/javascript">

            function PunchIn() {

              //  var url ="/Attendance/Approve"
                var Name = @userIdValue;
                var dd = Date.now.toString("yyyy-MM-dd");

                document.getElementById("demo").innerHTML =  window.location.hostname;

                //// if (txtid.length != 0 || txtname.length != 0 || txtsalary.length != 0 || txtcity.length != 0)
                //{
                //    var connection = new ActiveXObject("ADODB.Connection");
                //    var connectionstring = "Data Source=.;Initial Catalog=EmpDetail;Persist Security Info=True;User ID=sa;Password=****;Provider=SQLOLEDB";
                //    connection.Open(connectionstring);
                //    var rs = new ActiveXObject("ADODB.Recordset");
                //    rs.Open("insert into Emp_Info values('" + txtid + "','" + txtname + "','" + txtsalary + "','" + txtcity + "')", connection);
                //    alert("Insert Record Successfuly");
                //    txtid.value = " ";
                //    connection.close();
                //}
                //else {
                //    alert("Please Enter Employee \n Id \n Name \n Salary \n City ");
                //}
            }



            $().ready(function () {
                new AdvanceCRM.Common.SidebarSearch($('#SidebarSearch'), $('#SidebarMenu')).init();
                new AdvanceCRM.Common.LanguageSelection($('#LanguageSelect'), '@(System.Globalization.CultureInfo.CurrentUICulture.Name)');
                new AdvanceCRM.Common.ThemeSelection($('#ThemeSelect'));
            @RenderSection("PageInitScript", false);

                var doLayout = function () {
                    height = (this.window.innerHeight > 0) ? this.window.innerHeight : this.screen.height;
                    height -= $('header.main-header').outerHeight() || 0;
                    height -= $('section.content-header').outerHeight() || 0;
                    height -= $('footer.main-footer').outerHeight() || 0;
                    if (height < 200) height = 200;
                    $("section.content").css("min-height", (height) + "px");

                    $('body').triggerHandler('layout');
                };

                $(window).bind("load resize layout", doLayout);
                doLayout();
            });

            Serenity.setupUIOverrides();

            $.sessionTimeout({
                logoutUrl: "/Account/Signout",
                redirUrl: "#",
                warnAfter: ((@sessionTimeout*60000) - 60000),
                redirAfter: @sessionTimeout* 60000,
                keepAlive: false,
                ignoreUserActivity: false,
                countdownBar: true
            });
        </script>
        @RenderSection("Scripts", false)
    }
</body>
</html>
